apiVersion: ratelimit.solo.io/v1alpha1
kind: RateLimitConfig
metadata:
  name: gateway-level-rate-limit-config
  namespace: agentgateway-system
spec:
  raw:
    descriptors:
    - key: generic_key
      value: gateway-level-rate-limit
      rateLimit:
        requestsPerUnit: 10
        unit: HOUR
    rateLimits:
    - actions:
      - genericKey:
          descriptorValue: gateway-level-rate-limit
      type: TOKEN
---
apiVersion: enterpriseagentgateway.solo.io/v1alpha1
kind: EnterpriseAgentgatewayPolicy
metadata:
  name: gateway-level-rate-limit
  namespace: agentgateway-system
spec:
  targetRefs:
  - name: agentgateway-proxy
    group: gateway.networking.k8s.io
    kind: Gateway
  traffic:
    entRateLimit:
      global:
        rateLimitConfigRefs:
        - name: gateway-level-rate-limit-config
